services:
  gethomepage:
    image: ghcr.io/gethomepage/homepage:latest
    network_mode: bridge
    container_name: homepage
    hostname: homepage
    environment:
      HOMEPAGE_ALLOWED_HOSTS: nextmeats.asia, homepage.picityvietnam.com
      PGID: ${PGID}
      PUID: ${PUID}
      TZ: ${TZ}
    ports:
      - target: 3000
        published: "3020"
        protocol: tcp
    volumes:
      - type: bind
        source: "${BASEPATH}/${APP_CONFIG_PATH}"
        target: /app/config
      - type: bind
        source: /mnt
        target: /mnt
    restart: unless-stopped
    labels:
      proxy.aliases: homepage.picityvietnam.com
      proxy.#1.port: 3020
      