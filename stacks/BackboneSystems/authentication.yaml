services:
  pocketid:
    image: ghcr.io/pocket-id/pocket-id:v1
    network_mode: bridge
    hostname: pocketid
    container_name: pocketid
    environment:
      TZ: ${TZ}
      PGID: ${PGID}
      PUID: ${PUID}
      TRUST_PROXY: "true"
      ENCRYPTION_KEY: ${SECRET}
      APP_URL: "https://pocketid.${CF_DOMAIN}"
      INTERNAL_APP_URL: "http://172.16.1.122:1411"
      DB_PROVIDER: "postgres"
      DB_CONNECTION_STRING: "postgresql://postgres:postgres@172.16.1.122:${POSTGRES_PORT}/postgres"
      MAXMIND_LICENSE_KEY_FILE: "/app/data/${MAXMIND_LICENSE_KEY_FILE}"
    ports:
      - target: 1411
        published: 1411
        protocol: tcp
    volumes:
      - type: bind
        source: "${BASEPATH}/${APP_CONFIG_PATH}"
        target: /app/data
    restart: unless-stopped
